# Test dependencies for query service
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0

# Service clients (used in integration tests)
neo4j>=5.18
qdrant-client~=1.13.0
httpx>=0.27

# FastAPI + test client (needed for integration tests)
fastapi[standard]>=0.115
pydantic>=2.0

# Type checking
mypy>=1.9
types-requests

# Formatting / linting
black>=24.0
ruff>=0.4
